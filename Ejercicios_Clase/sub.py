''' def buscarSubcadena(self,sub):
    auxcs,cc,cs= 0,0,0
    buscado=False
    self.dato= self.dato.lower()
    while (cc<len(self.dato) and buscado==False):
        if (sub[0]==self.dato[cc]):
            cs=0
            auxcs=cc
            while (cs<len(sub) and cc< '''
            
cont= 0
n=0
while n %5==0:
        cont= cont+n
print(cont) 